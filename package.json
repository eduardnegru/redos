{
  "name": "redos",
  "version": "1.0.0",
  "description": "Regex DoS attack demo",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js vulnerable",
    "v": "nodemon server.js vulnerable",
    "n": "nodemon server.js not-vulnerable"
  },
  "author": "Adrian",
  "license": "ISC",
  "dependencies": {
    "connect-timeout": "^1.9.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.7"
  }
}
